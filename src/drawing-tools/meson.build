current_dir = 'drawing-tools'

paint_sources += [
  current_dir / 'brush.c',
  current_dir / 'circle.c',
  current_dir / 'color-picker.c',
  current_dir / 'fill.c',
  current_dir / 'line.c',
  current_dir / 'rectangle.c',
  current_dir / 'select.c',
  current_dir / 'text.c',
]
